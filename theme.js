import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  h1: {
    color: 'white',
    marginTop: 150,
    fontSize: 50,
    fontFamily: 'Special Elite',
    textAlign: 'center'
  }
};
